import React from 'react';
import './spinner.css';

const Spinner = () => (
  <div className="spinnerContainer">
    <div className="spinner">
      <div />
    </div>
  </div>
);

export default Spinner;
